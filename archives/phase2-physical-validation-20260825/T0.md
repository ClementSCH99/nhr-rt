# T0 — Environments and software baseline

> Archived campaign record. Referenced raw files were removed during closeout;
> see [archive notes](README.md).

- Campaign ID: `PHASE2-PHYSICAL-20260825-NHR79503`
- Date: 2026-08-25 (America/Toronto)
- Authorization: Tier S only; no Git command; no physical or IVI access
- Repository branch: `codex/milestone-4-readonly-web-monitor`
- Repository commit: `6f335a995bea255062d05d8458074d58e5880ecb`
- Working-tree state: not clean; modified `.gitignore` and `README.md`, untracked
  `docs/PHASE2_PHYSICAL_VALIDATION_PLAN.md` and `service.local.json`
- Disposition: `PASS`

## Environment evidence

- Host: Windows
- 32-bit environment: `.venv32`, Python 3.12.10, 32-bit
- 64-bit environment: `.venv64`, Python 3.12.10, 64-bit
- Both environments resolve `nhr9300` to the current workspace through an
  editable installation.
- 32-bit editable IVI/service reinstall: PASS; `comtypes==1.4.16` and
  `PyYAML==6.0.3` present.
- 64-bit runtime separation: PASS; monitor/client ran without loading
  `comtypes`.
- Deviation: a 64-bit editable reinstall could not be reproduced because
  `setuptools.build_meta` is absent from `.venv64`; the existing editable
  installation and console entry point remained functional.

## Automated regression

Command:

```powershell
.\.venv32\Scripts\python.exe -m pytest -q -p no:cacheprovider --basetemp=.test-temp-final-2
```

Result: `121 passed, 2 skipped in 47.21s`. The two skipped tests are the
explicitly gated hardware read-only and hardware energizing tests. No hardware
opt-in option or environment acknowledgement was supplied.

An earlier run under a restricted OneDrive-local temporary directory produced
pytest temporary-directory permission errors and was discarded as
non-functional infrastructure evidence before the clean rerun above.

## Simulator, client, runtime and SSE

- 32-bit service started from `examples/service.simulator.json` on
  `127.0.0.1:9300` with `sim-1` and `sim-2`.
- 64-bit client inventory/configuration reads succeeded.
- `sim-1` connected in simulator only; output remained disabled, all setpoint
  channels disabled and all numeric setpoints zero.
- Runtime schema `1.0`: measurement available/fresh, acquisition healthy,
  no alerts, workflow idle, external sources and dynamic power contribution
  `not_configured`.
- Runtime SSE returned a schema `1.0` typed event with matching event name.
- The 64-bit process did not load `comtypes`.

## Monitor consumer validation

- 64-bit monitor bound only to `127.0.0.1:9400` and rendered the packaged
  localhost page in the in-app browser.
- Visual state: `READ ONLY`, service live, `STANDBY`, output disabled,
  measurement fresh, evidence health `OK`, external/SoP not configured.
- No connect, disconnect, start, stop or setpoint control was present.
- `GET /api/config`, `GET /api/runtime` and `HEAD /` returned success;
  `POST /api/runtime` returned HTTP 405.
- Closing the browser page and then the monitor process did not alter the
  simulator instrument state or stop service-owned acquisition.

## Durable acquisition evidence

- CSV: `runs/sim-1_20260825T144933_404105Z.csv`
- CSV SHA-256:
  `69C91ABD25A914726BE09FC63C1403A428073036163FB97C05B2EF497383569A`
- Rows: 599
- UTC duration: 119.604389 s
- Effective rate: 4.9998165 Hz
- Mean/median interval: 0.2000167 s / 0.2030000 s
- Interval range: 0.1720000 s to 0.2340000 s
- States: `standby` only
- Interlocks: `ok` only
- Errors: 0
- Voltage/current/power: 350.0 V / 0.0 A / 0.0 W throughout
- Non-zero setpoint rows: 0

Analysis script: `runs/tier_s_evidence_analysis.py`.

## Cleanup and decision

- Monitor received Ctrl+C and logged an operator-requested shutdown.
- Service received Ctrl+C, logged closing and `NHR9300 service stopped`.
- Final port inspection showed no `LISTEN` entry on 9300 or 9400; remaining
  entries were `TIME_WAIT` only.
- This is software/simulator/monitor evidence only. It is not IVI, physical
  output, watchdog, electrical or battery validation.
- Repository identity and the non-clean working tree are recorded above.
- The missing 64-bit build tooling remains a non-blocking environment
  reproducibility deviation because the existing editable installation,
  public client and monitor entry point all executed successfully.
